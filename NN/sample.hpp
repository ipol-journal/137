#ifndef SAMPLE_HPP
#define SAMPLE_HPP

#include "imageio.hpp"

void sample(
        const char * path,
        const char * outFile,
        patchConfig const & patchSize,
        size_t nSize
);

#endif
